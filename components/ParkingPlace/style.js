import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    parkingPlace: {
        alignItems: 'center',
        position: 'relative',
        marginTop: '5%',
        width: '100%',
        borderWidth: 3,
        borderColor: '#008eab', 
        borderRadius: 10,
        
      },
    placeNumber:{
        fontSize: 20,
    },
});

export default styles;
